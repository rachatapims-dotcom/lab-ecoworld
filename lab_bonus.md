# 🏆 Bonus Mission: The Indecisive Client (ลูกค้าจอมลังเล)

**เงื่อนไข:** ทำภารกิจนี้หลังจาก Merge งานหลักเสร็จสิ้นแล้ว
**ผู้รับผิดชอบ:** Student A (หรือใครก็ได้ 1 คน)

## 📝 สถานการณ์
ลูกค้าสั่งแก้เว็บด่วน! เดี๋ยวก็อยากได้ เดี๋ยวก็ไม่เอา (Undo/Redo) ให้คุณใช้ Git จัดการปัญหานี้โดย **ห้ามลบโค้ดด้วยมือ**

---

## 🔧 ขั้นตอนภารกิจ

### 1. ลูกค้าสั่งเพิ่มแบนเนอร์ (The Mistake)
* เปิดไฟล์ `index.html` เพิ่มโค้ดนี้ไว้บนสุด (ใต้ `<body>`):
  ```html
  <div style="background-color: red; color: white; padding: 20px; text-align: center;">
      🔥 โปรโมชั่นลดโลกร้อน 50% วันนี้วันเดียว!
  </div>
  ```

### 2. ลูกค้าเปลี่ยนใจ (Undo) 
* ใช้คำสั่ง `git reset --hard HEAD~1` เพื่อย้อนกลับไป Commit ก่อนหน้า (การเพิ่มแบนเนอร์)

### 3. ลูกค้าเปลี่ยนใจอีกรอบ (Redo) 
* ใช้คำสั่ง `git reflog` เพื่อดูประวัติการเปลี่ยนแปลงทั้งหมด
* หา Commit Hash ของการเพิ่มแบนเนอร์
* ใช้คำสั่ง `git cherry-pick [Commit Hash]` เพื่อนำ Commit นั้นกลับมาอีกครั้ง

---

## ✅ Checklist ส่งงาน Bonus
- [ ] แสดงการใช้ `git reset --hard`
- [ ] แสดงการใช้ `git reflog` และ `git cherry-pick`
